### THIS FILE IS GENERATED AUTOMATICALLY
### DO NOT MODIFY IT DIRECTLY
### USE ./build-ci.sh INSTEAD

stages:
  - build
  - test
  - release

before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS

variables:

  IMAGE: dbogatov/docker-images

  DEBIAN_TEST: $IMAGE:debian-$CI_BUILD_REF_NAME
  DEBIAN_RELEASE: $IMAGE:debian-latest

  JEKYLL_TEST: $IMAGE:jekyll-$CI_BUILD_REF_NAME
  JEKYLL_RELEASE: $IMAGE:jekyll-latest

  MK_DOCS_TEST: $IMAGE:mk-docs-$CI_BUILD_REF_NAME
  MK_DOCS_RELEASE: $IMAGE:mk-docs-latest

  NET_CORE_TEST: $IMAGE:net-core-$CI_BUILD_REF_NAME
  NET_CORE_RELEASE: $IMAGE:net-core-latest

  NGINX_TEST: $IMAGE:nginx-$CI_BUILD_REF_NAME
  NGINX_RELEASE: $IMAGE:nginx-latest


### DEBIAN

debian-build:
  stage: build
  script:
    - docker build --pull -t $DEBIAN_TEST images/
    - docker push $DEBIAN_TEST
  tags:
    - shell

debian-test:
  stage: test
  script:
    - docker pull $DEBIAN_TEST
    - docker run -v $CI_PROJECT_DIR/images//test.sh:/test.sh --entrypoint="/bin/bash" $DEBIAN_TEST /test.sh
  tags:
    - shell

debian-release:
  stage: release
  script:
    - docker pull $DEBIAN_TEST
    - docker tag $DEBIAN_TEST $DEBIAN_RELEASE
    - docker push $DEBIAN_RELEASE
  only:
    - master
  tags:
    - shell


### JEKYLL

jekyll-build:
  stage: build
  script:
    - docker build --pull -t $JEKYLL_TEST images/
    - docker push $JEKYLL_TEST
  tags:
    - shell

jekyll-test:
  stage: test
  script:
    - docker pull $JEKYLL_TEST
    - docker run -v $CI_PROJECT_DIR/images//test.sh:/test.sh --entrypoint="/bin/bash" $JEKYLL_TEST /test.sh
  tags:
    - shell

jekyll-release:
  stage: release
  script:
    - docker pull $JEKYLL_TEST
    - docker tag $JEKYLL_TEST $JEKYLL_RELEASE
    - docker push $JEKYLL_RELEASE
  only:
    - master
  tags:
    - shell


### MK-DOCS

mk-docs-build:
  stage: build
  script:
    - docker build --pull -t $MK_DOCS_TEST images/
    - docker push $MK_DOCS_TEST
  tags:
    - shell

mk-docs-test:
  stage: test
  script:
    - docker pull $MK_DOCS_TEST
    - docker run -v $CI_PROJECT_DIR/images//test.sh:/test.sh --entrypoint="/bin/bash" $MK_DOCS_TEST /test.sh
  tags:
    - shell

mk-docs-release:
  stage: release
  script:
    - docker pull $MK_DOCS_TEST
    - docker tag $MK_DOCS_TEST $MK_DOCS_RELEASE
    - docker push $MK_DOCS_RELEASE
  only:
    - master
  tags:
    - shell


### NET-CORE

net-core-build:
  stage: build
  script:
    - docker build --pull -t $NET_CORE_TEST images/
    - docker push $NET_CORE_TEST
  tags:
    - shell

net-core-test:
  stage: test
  script:
    - docker pull $NET_CORE_TEST
    - docker run -v $CI_PROJECT_DIR/images//test.sh:/test.sh --entrypoint="/bin/bash" $NET_CORE_TEST /test.sh
  tags:
    - shell

net-core-release:
  stage: release
  script:
    - docker pull $NET_CORE_TEST
    - docker tag $NET_CORE_TEST $NET_CORE_RELEASE
    - docker push $NET_CORE_RELEASE
  only:
    - master
  tags:
    - shell


### NGINX

nginx-build:
  stage: build
  script:
    - docker build --pull -t $NGINX_TEST images/
    - docker push $NGINX_TEST
  tags:
    - shell

nginx-test:
  stage: test
  script:
    - docker pull $NGINX_TEST
    - docker run -v $CI_PROJECT_DIR/images//test.sh:/test.sh --entrypoint="/bin/bash" $NGINX_TEST /test.sh
  tags:
    - shell

nginx-release:
  stage: release
  script:
    - docker pull $NGINX_TEST
    - docker tag $NGINX_TEST $NGINX_RELEASE
    - docker push $NGINX_RELEASE
  only:
    - master
  tags:
    - shell

